# Raven Ledger Ingestion API

This API é a component of RavenLedger Service, which purpose is to receive external events by HTTP, check if sender is allowed to send this event, check and grant the event contract.